version: '2'
services:
  # Primary Tower Development Container link
  awx:
    links:
      - httpbin
  httpbin:
    image: kennethreitz/httpbin
    container_name: tools_httpbin_1
    ports:
      - '8204:80'
