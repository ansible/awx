---
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    isDefault: true
    access: proxy
    url: http://prometheus:9090
    editable: true
    jsonData:
      timeInterval: 5s
    uid: awx_prometheus
