---
- name: Run Integration Test
  hosts: localhost
  connection: local
  gather_facts: False
  environment:
      TOWER_HOST: https://10.42.42.239/
      TOWER_USERNAME: admin
      TOWER_PASSWORD: password
      TOWER_VERIFY_SSL: False
  collections:
    - awx.awx

  tasks:
    - name: Include task list in play
      include_tasks: main.yml